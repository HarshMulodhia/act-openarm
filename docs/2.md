# Refactor Standards and Checklist

## Refactor Goals

- Improve clarity and maintainability of project documentation.
- Align repository guidance with the actual workspace structure from `tree.log`.
- Remove references to directories or tools not present in the workspace.

## Standards

1. **Single Source of Truth**
   - Keep canonical architecture and workflow information in stable docs.
2. **Separation of Concerns**
   - Distinguish upstream dependencies from project-owned integration code in `openarm_act_project/`.
3. **Security Hygiene**
   - Do not include sensitive links, leaked key examples, or unsafe references.
4. **Reproducibility**
   - Document assumptions, required repositories, and execution order.
5. **Actionability**
   - Prefer concise instructions over lengthy generated narratives.

## Implementation Checklist

- [x] Create integration package layout (`openarm_act_project/src/openarm_act/`).
- [x] Add `__init__.py` for every sub-package (`datasets/`, `envs/`, `policies/`, `utils/`).
- [x] Add dataset adapter (`datasets/openarm_isaac_dataset.py`).
- [x] Add environment wrapper (`envs/openarm_isaac_env.py`).
- [x] Add policy wrapper (`policies/act_openarm_policy.py`).
- [x] Add utility modules (`utils/io_utils.py`, `utils/viz_utils.py`).
- [x] Implement lifecycle scripts (collect, train, eval, deploy).
- [x] Add reach task config (`configs/act_openarm_reach.yaml`).
- [x] Add lift task config (`configs/act_openarm_lift.yaml`).
- [x] Add `setup.py` for installable package.
- [x] Create checkpoint directories (`checkpoints/openarm_reach_act/`, `checkpoints/openarm_lift_act/`).
- [x] Define and document dataset schema end-to-end (see `docs/pipeline.md` §4).
- [x] Add logging and error handling for all entry points.
- [x] Add complete pipeline guide (`docs/pipeline.md`).
- [ ] Validate workflow end-to-end in simulation.

## Done Criteria

- Documentation is UTF-8 encoded and human-maintainable.
- Each doc has a clear purpose with minimal overlap.
- Workflow can be followed without external hidden context.
- Directory names in docs match the actual workspace (`isaaclab/`, `openarm_isaac_lab/`).
